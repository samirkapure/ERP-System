<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.ui">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/uiicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity2" />
        <activity android:name=".LeaveApplication" />
        <activity android:name=".SignUp" />
        <activity android:name=".monthlyUnderTaking" />
        <activity android:name=".AdminView" />
        <activity android:name=".ViewLa" />
        <activity android:name=".ViewMu" />
        <activity android:name=".feedback" />
        <activity android:name=".ViewFd"></activity>
    </application>

</manifest>